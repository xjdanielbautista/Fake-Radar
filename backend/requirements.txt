# ===================================================================
# Fake Radar API — requirements.txt
# ===================================================================

# Servidor y framework
fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.3
python-dotenv==1.0.1

# Google Generative AI SDK
google-genai

# Motor FakeRadar V5 (RoBERTa)
# Versiones fijadas para evitar conflictos transformers/peft en Python 3.12
transformers==4.47.0
accelerate==1.2.0

# PyTorch CPU — versión estable existente
# Para GPU en producción cambiar a: torch==2.6.0+cu121
torch==2.6.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# Numpy (requerido por transformers y torch)
numpy>=1.24.0,<2.0.0